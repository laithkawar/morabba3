A TailwindCSS-Vue landing page for an automated Arabic logomaker

To start the development server:

```bash
# Using npm
npm run dev

Now you should be able to see the project running at localhost:3000.
```
